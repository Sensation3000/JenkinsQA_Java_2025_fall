package school.redrover.page;

import org.openqa.selenium.WebDriver;
import school.redrover.common.BasePage;

public class BaseConfigurationPage extends BasePage {

    public BaseConfigurationPage(WebDriver driver) {
        super(driver);
    }
}
